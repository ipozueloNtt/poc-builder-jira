export type CardSelectableSizes = 'sm' | 'md';
export type CardSelectableTypes = 'single' | 'multiselect';

export enum CardSelectableSize {
  Small = 'sm',
  Medium = 'md',
}

export enum CardSelectableType {
  Single = 'single',
  Multiselect = 'multiselect',
}
